#!/usr/bin/env bash

if ! command -v bundle &> /dev/null; then
  echo "Error: bundle is not installed." >&2
  exit 1
fi

load_dotenv() {
  set -o allexport
  [[ -f .env ]] && source .env
  set +o allexport
}

load_dotenv
bundle exec fastlane "$@"
